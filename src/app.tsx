import * as React from "react";

const Title = () => <h1>Welcome to <span>Memory Game</span></h1>;

const GameBoard = () => <div/>;

const App = () => (
    <div>
        <Title/>
        <GameBoard/>
    </div>
);

export default App;
